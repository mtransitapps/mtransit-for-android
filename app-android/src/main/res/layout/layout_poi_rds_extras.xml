<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/extra"
    android:layout_width="@dimen/poi_extra_width"
    android:layout_height="@dimen/poi_extra_height"
    android:layout_alignParentStart="true"
    android:layout_marginEnd="@dimen/poi_extra_margin_end"
    android:visibility="gone"
    tools:background="#00FF00"
    tools:showIn="@layout/layout_poi_rds_with_schedule"
    tools:visibility="visible">
    <!-- ROUTE DIRECTION -->
    <LinearLayout
        android:id="@+id/route_direction"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/route_direction_gradient_rounded"
        android:orientation="vertical">
        <!-- ROUTE -->
        <LinearLayout
            android:id="@+id/route"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="3dp"
            android:layout_marginBottom="-4dp"
            android:layout_weight="1"
            android:gravity="center"
            android:orientation="horizontal"
            android:weightSum="4"
            tools:background="#FF00FF">
            <!-- ROUTE TYPE IMG -->
            <org.mtransit.android.ui.view.MTJPathsView
                android:id="@+id/route_type_img"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_gravity="center"
                android:layout_weight="4"
                android:padding="4dp"
                android:visibility="gone"
                tools:background="@drawable/ic_directions_subway_black_24dp"
                tools:layout_weight="2"
                tools:visibility="visible" />
            <!-- ROUTE SHORT NAME -->
            <TextView
                android:id="@+id/route_short_name"
                style="@style/MTTextRouteShortName"
                android:visibility="gone"
                tools:background="#FF00FF"
                tools:layout_width="wrap_content"
                tools:text="RSN1"
                tools:visibility="visible" />
        </LinearLayout>
        <!-- DIRECTION HEADING -->
        <FrameLayout
            android:id="@+id/direction_heading_bg"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:visibility="gone"
            tools:visibility="visible">
            <!-- DIRECTION HEADING TEXT -->
            <TextView
                android:id="@+id/direction_heading"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_horizontal"
                android:layout_marginStart="@dimen/baseline_2"
                android:layout_marginEnd="@dimen/baseline_2"
                android:ellipsize="marquee"
                android:includeFontPadding="true"
                android:marqueeRepeatLimit="marquee_forever"
                android:paddingBottom="@dimen/baseline_2"
                android:singleLine="true"
                android:textAppearance="?attr/textAppearanceHeadline2"
                android:textColor="@android:color/white"
                android:textSize="@dimen/head_sign_text_size_short"
                android:textStyle="bold"
                tools:fontFamily="sans-serif-condensed"
                tools:text="&#10132;&#x200A;Direction direction" />
        </FrameLayout>
    </LinearLayout>
    <!-- ROUTE/DIRECTION SERVICE UPDATES -->
    <include layout="@layout/layout_route_direction_service_update_img" />
    <!-- DEBUG center alignment
    <View
        android:layout_width="0.5dp"
        android:layout_height="match_parent"
        android:layout_gravity="center_horizontal"
        android:alpha="0.3"
        android:background="#000000" />

    <View
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:layout_gravity="center_vertical"
        android:alpha="0.3"
        android:background="#000000" />
    DEBUG -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="?android:attr/selectableItemBackground" />
</FrameLayout>
