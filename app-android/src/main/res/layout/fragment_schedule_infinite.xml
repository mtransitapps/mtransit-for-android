<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/context_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.schedule.ScheduleFragment">

    <include
        android:id="@+id/screen_toolbar_layout"
        layout="@layout/layout_screen_toolbar" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior">

        <!-- LOADING -->
        <include
            android:id="@id/loading_layout"
            layout="@layout/layout_loading_large"
            android:visibility="gone"
            tools:visibility="visible" />
        <!-- LIST -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@id/list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:visibility="visible"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            tools:listitem="@layout/layout_schedule_infinite_list_time"
            tools:visibility="visible" />

        <!-- ON TOP -->
        <TextView
            android:id="@+id/local_time"
            style="@style/MTTextSourceLabel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="top"
            android:layout_marginTop="@dimen/baseline_40"
            android:gravity="end"
            android:paddingHorizontal="@dimen/baseline_8"
            android:paddingVertical="@dimen/baseline_2"
            android:visibility="gone"
            tools:text="Local time: 00:00"
            tools:visibility="visible" />

        <TextView
            android:id="@+id/source_label"
            style="@style/MTTextSourceLabel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:paddingHorizontal="@dimen/baseline_8"
            android:paddingVertical="@dimen/baseline_2"
            android:visibility="gone"
            tools:text="Source: api.com"
            tools:visibility="visible" />

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fab_real_time"
            style="@style/MTFabMiniBottomEnd"
            android:layout_marginBottom="@dimen/baseline_12_x"
            android:contentDescription="@string/fab_action_real_time_hide"
            android:src="@drawable/ic_baseline_wifi_off_24"
            android:visibility="gone"
            tools:backgroundTint="#00FF00"
            tools:contentDescription="@string/fab_action_real_time_show"
            tools:layout_marginBottom="@dimen/baseline_64"
            tools:rippleColor="#FF00FF"
            tools:src="@drawable/ic_baseline_wifi_24"
            tools:visibility="visible" />

    </FrameLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>